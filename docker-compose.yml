services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: auv
      POSTGRES_PASSWORD: auv
      POSTGRES_DB: auv
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
