# ROS2 workspace (optional)

If you want ROS2 pub/sub between sensors, agent, and control:

- Create a ROS2 workspace here (e.g., Humble).
- Add nodes:
  - sensor publishers (sonar/imu/chemistry)
  - controller subscriber (VehicleCommand)

This repo keeps ROS2 optional so Windows dev still works.
